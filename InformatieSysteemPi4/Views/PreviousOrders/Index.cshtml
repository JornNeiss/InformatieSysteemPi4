@model IEnumerable<InformatieSysteemPi4.Models.PreviousOrder>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IsRepeatable)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Customer)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IsRepeatable)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Customer.CustomerID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order.OrderID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PreviousOrderID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PreviousOrderID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PreviousOrderID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
